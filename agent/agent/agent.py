import os
import subprocess


def get_metrics():
    pass

    #
    # VM_HOST = os.getenv('VM_HOST')
    # VM_PORT = os.getenv('VM_PORT')
    # VM_PROTOCOL = os.getenv('VM_PROTOCOL')
    #
    # cmd = f"prom2json;{VM_PROTOCOL}://{VM_HOST}:{VM_PORT}/metrics"
    # json_data = subprocess.run()
